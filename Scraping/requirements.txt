requests>=2.31.0
geopandas>=0.14.0
h3>=4.0.0
shapely>=2.0.0
pyrosm>=0.6.2
rasterio>=1.3.0
rasterstats>=0.19.0

# Catatan: skrip generate_h3_grid.py sudah kompatibel h3-py v3 maupun v4
# (auto-detect versi terinstall). Kalau kamu sudah kadung install h3 v3.7.x
# (API lama: polyfill, h3_to_geo_boundary), tetap jalan tanpa perlu upgrade.
#
# pyrosm dibutuhkan untuk fetch_osm_geofabrik.py (jalur cadangan kalau
# Overpass API publik gagal terus - lihat komentar di file itu).
#
# rasterio + rasterstats dibutuhkan untuk compute_worldpop_zonal.py.
# Kalau instalasi rasterio lewat pip bermasalah (butuh GDAL binary),
# install lewat conda: conda install -c conda-forge rasterio rasterstats